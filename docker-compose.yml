services:
  app:
    build: .
    env_file: .env
    environment:
      REDIS_URL: redis://127.0.0.1:6379
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G
